function create_learning_online(e) {
    e.preventDefault();

    const LOClass = $('#create_video_type').val();
    const Video_Name = $('#create_video_title').val();

    console.log(LOClass);
    console.log(Video_Name);

    //待宇蓁補
    // var video_up = $("#video_up").prop('files');
    // var API = "http://192.168.43.56:5002/api/learnonline/learnonlineupload";
    // var form_data = new FormData();

    // var file = document.getElementById("video_up");
    // if (file.length < 1) {
    //     alert('請選擇影片檔!!');
    //     return false;
    // }
    // console.log(video_up);

    // for (let i = 0; i < for_doc.length; i++) {
    //     form_data.append('UploadFile', for_doc[i]);
    // }

}